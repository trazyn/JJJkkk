
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 24,
        paddingTop: 10,
        paddingBottom: 0,
        marginBottom: 32,
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
    },

    done: {
        color: '#405DE6',
        fontSize: 20,
    },
});
